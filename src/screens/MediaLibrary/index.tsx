import React from 'react';
import { View, Text } from 'react-native';

const MediaLibrary = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>MediaLibrary Screen</Text>
    </View>
  );
};

export default MediaLibrary;
